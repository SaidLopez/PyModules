"""Namespace marker for the contrib tracing exporters.

Imports nothing by default — concrete exporters (e.g.
``pymodules.contrib.tracing.opentelemetry``) live in their own modules
and pull in their own optional dependencies on demand.
"""
